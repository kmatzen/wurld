name: test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch: {}

jobs:
  python:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python: ["3.11", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Install ffmpeg (Stray importer tests)
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then sudo apt-get update && sudo apt-get install -y ffmpeg
          else brew install ffmpeg; fi
      - name: Install
        run: pip install -e ".[dev,mcap,record3d,euroc,ros2]"
      - name: Test
        run: pytest tests -q

  cpp:
    # The C++ reader is checked against the Python one, so this job needs both.
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install
        run: pip install -e ".[dev]"
      - name: Build and unit-test the C++ reader
        run: |
          cmake -S cpp -B cpp/build -DCMAKE_BUILD_TYPE=Release
          cmake --build cpp/build -j
          ./cpp/build/wurld_test
      - name: Cross-implementation parity against the Python reader
        run: pytest tests/test_cpp_reader.py -q
      - name: C++ writer produces files Python validates
        run: pytest tests/test_cpp_writer.py -q
      - name: C++ streaming writer against real encoder chunks
        run: pytest tests/test_cpp_stream.py -q
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Conformance corpus across all three readers
        run: pytest tests/test_conformance.py -q

  viewer-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm ci
      - name: wurld.js parity checks
        run: npm test
      - name: package publishes cleanly
        run: |
          npm pack
          tar tzf wurld-core-*.tgz

  # The viewer decodes through chromapakz, which needs WebCodecs, and draws into
  # 2D canvases and WebGL — none of which jsdom has. Testing it means running it.
  # Five defects were found in this page by hand before it had any test at all.
  viewer-browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm ci
      - name: install chromium
        run: npx playwright install --with-deps chromium
      - name: viewer behaviour in a real browser
        run: npm run test:viewer
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-trace
          path: |
            test-results/
            playwright-report/
          retention-days: 7
